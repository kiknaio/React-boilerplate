import React from 'react';
import ReactDOM from 'react-dom';

export default class CustomComponent extends React.Component {
  render() {
    return (
       <h2>Just another custom component</h2>
    );
  }
};
